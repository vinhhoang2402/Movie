<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <import type="com.example.movie.model.Genress.Genres"/>
        <variable
            name="genres"
            type="Genres" />

    </data>

    <LinearLayout
        android:id="@+id/llTypeProject"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="14dp"
        android:background="@drawable/bg_rectangle_stroke_a4a4a4"
        android:gravity="center">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/tvTypeProject"
            style="@style/StyleTextView.Normal.Silver"
            android:paddingStart="12dp"
            android:paddingTop="6dp"
            android:paddingEnd="12dp"
            android:paddingBottom="6dp"
            android:text="@{genres.name}" />
    </LinearLayout>
</layout>